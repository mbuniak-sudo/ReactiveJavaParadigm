# Set default behavior for text files
* text=auto

# Force LF line endings for scripts (e.g., shell scripts)
*.sh text eol=lf

# Force CRLF line endings for Windows batch files
*.bat text eol=crlf
*.cmd text eol=crlf